# Hello, world

This is a placeholder essay. Drop `.md` files into the `essays/` folder — the first `# heading` becomes the title, the filename becomes the URL.

Delete this one when the real writing starts.
